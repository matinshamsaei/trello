import Vue from 'vue'
import Vuex from 'vuex'
import board from '../default-board'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    board
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
